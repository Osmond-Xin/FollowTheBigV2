Repo /Users/osmond/Documents/project/FollowTheBigV2, branch explore/赚钱模式. Run with `PYTHONPATH=src uv run python ...`. Do NOT modify src/, do NOT git commit, do not touch research/eval/. The spec you implemented is research/.specs/spec_flow.md; the file is research/panel/flow_day.py. It ran on 2025-03-12 (all symbols): read_t=67.9s ft=123.6s read_o=95.2s fo=84.8s total=372s (reads were slowed by another job on the same disk; the feature compute time ft+fo ≈ 208 s is the real problem). Output looks mostly right (buy+sell ≈ 0.98 × 当日成交额, buckets sum to 1, rates in [0,1]). Fix these defects, then re-run the same day and report the new timings and the transposed rows for 600519.SH and 000001.SZ:

1. `t_imb_open_call` and `t_imb_close_call` are null for ALL symbols and the corresponding amt shares are 0. The auction fills are NOT stamped exactly 09:25:00.000 / 15:00:00.000 — they carry millisecond offsets (e.g. SH 33900720 = 09:25:00.720). Define open-call fills as time_ms in [09:25:00, 09:30:00) and close-call fills as time_ms ≥ 14:57:00. Verify on the day that every symbol then gets non-null values and that open-call amt share is typically 1–10%.
2. `cx_amt_rate_b/_s` on SH are ~0.0003 while the count-based cancel rate is ~0.48 — almost certainly the SH 'D' (cancel) rows carry price 0 so cancelled amt is 0. Check it (group_by on SH D rows: how many have price == 0). Compute cancelled amt using the price from the joined original 'A' order (SH) / original order (SZ), i.e. amt_cancelled = cancel.vol × order.price/10000. Same fix for `cx_big_rate` if it uses cancel-row price.
3. Performance: profile `features_trades` and `features_orders` (time each block with time.perf_counter and print) and get compute to ≤ 60 s per day total. Likely culprits: the aggressor/passive order-level aggregation (do a single group_by over (symbol, aggressor_oid) with sums, not multiple passes), the sign autocorrelation (use shift over symbol on a pre-sorted frame; do not sort more than once), the cancel↔order join (join on (symbol, oid) with both sides pre-selected to the minimal columns; use Int64 keys; for SZ only trades rows with code == 'C' are cancels — filter before joining), and any `.pivot`/`.partition_by`/Python loops over symbols. Sort the raw frames once by (symbol, time_ms) and reuse. Use `pl.LazyFrame` + `collect()` per stage if it helps.
4. `t_run_mean` null for 8 symbols and `pre_cx_*` null for 110: acceptable if those symbols have no continuous-session trades / no pre-open orders — just confirm that is the reason.
5. Write `research/panel/README.md` (Chinese, short): one line per output column, the SH-vs-SZ caveats you verified (what bs means on SH; the SH residual-quantity issue; SH cancel rows' price), and the exact command to run the full build.
Do not launch the full build. Finish with a concise report (timings before/after, what you found for items 1–2 and 4).
