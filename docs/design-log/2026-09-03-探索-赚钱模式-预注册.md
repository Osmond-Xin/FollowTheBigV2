# 探索：赚钱模式（预注册，2026-09-03）

> 用户裁定：不限制手段，只用现有数据（1122 天三流 Level-2），目标只有一个——找到一个能赚钱的模式。
> 本文在**看到任何前向收益之前**写下判据。写完之后只追加，不改动。

## 一、判据（用户的五条，落成可算的东西）

一条信号要能跟，必须同时：可归因 · 未完成 · 在价格动之前 · 方向一致跨日持续 · 规模够。
本轮把它压成一个可执行的问题：**在 t 日收盘能算出来的量 X，能不能预测 t+1 开盘买入、持有 N 日的收益，扣掉成本还剩钱。**

## 二、样本、宇宙、成本、方向（先写死）

- **样本切分**：样本内 2022-01-04 → 2024-12-31；**样本外 2025-01-01 → 2026-08-20**。样本外只看一次，看之前不许改因子定义。
- **宇宙**：主板；近 20 日日均成交额 ≥ 1000 万元；上市 ≥ 60 个交易日；当日快照数 ≥ 1000（非停牌）；
  当日未封涨跌停（无卖一/无买一的快照占比 < 50% 且 |日收益| < 9.5%）；次日开盘未涨跌停（可执行）。
- **收益**：`fwdo_N` = 次日开盘买入到 t+N 收盘（N ∈ 1, 2, 5, 10, 20）；同时报 `fwd_N`（收盘到收盘）作对照。
- **成本**：单边佣金 0.025% ×2 + 印花税 0.05% + 滑点 0.05% = 往返 0.15%；净收益 = 毛 − 换手 × 成本。
- **方向**：**每个因子两个方向都报，不事后翻号**。ICIR 为负一样是发现（Fade）。
- **多重检验**：本轮测的因子总数在结果里写明；用 |ICIR| 排序后，**只有同时在样本内、样本外同号且样本外 NW-t ≥ 2 的才算候选**。

## 三、假设清单（H1–H8），来源与预期方向

| # | 因子 | 只有 Level-2 才算得出？ | 来源 | 事先预期 |
|---|---|---|---|---|
| H1 | 全簿买卖失衡均值 `imb_total_mean`、尾盘 `imb_total_last15` | 是（叫买/叫卖总量是全簿） | 订单簿失衡预测短期收益（经典） | 正，短期（1–5 日） |
| H2 | **订单级**主动大单净流 `o_net_b4/b5`（按主动方委托号聚合） | 是 | 开源「大小单重定标」；文献说成交级失效、订单级未知 | 未知，双向报 |
| H3 | **被动**大单净成交 `p_net_b4/b5`（大挂单被吃掉 = 有人耐心吸/派） | 是 | 本项目「吸筹」直觉 | 未知；Bucci/DPV 暗示 Fade |
| H4 | 撤单行为 `cx_hf_b`、`pre_cx_b`、`cx_rate_b` | 是 | 开源 IC 0.045；东方盘前撤单 | 报里为正 |
| H5 | 收盘集合竞价失衡 `t_imb_close_call`、`imb_total_closecall` | 是 | 收盘竞价失衡预测隔夜（美股文献） | 正，N=1 |
| H6 | 行为指纹 `f_round500`、`qf_round500_b` | 是 | 「500 股整数倍比金额阈值有效」 | 未知 |
| H7 | 成交方向持续性 `t_sign_ac1`、`t_run_mean`（母单在执行） | 是 | Bucci 冲击 50 日衰减 | 负（Fade） |
| H8 | **静默吸筹事件**：`p_net_b5` 当日前 5% ∧ \|ret1\| < 1% ∧ amt/amt20 > 1.5 | 是 | 「大资金在吃货而价没动」 | 正（跟）或负（Fade），事件研究双向报 |

对照基线（不用 Level-2 也能算）：`ret1`（反转）、`mom20`、`log_amt20`、`amt_share_last15`（光大 OCVP 类）。
**Level-2 因子必须在中性化（对 log_amt20、mom20、ret1）之后仍有 ICIR，否则不算 Level-2 的功劳。**

## 四、成功判据（写死）

- 候选因子：样本内 |ICIR| ≥ 0.3 且 NW-t ≥ 3；样本外同号且 NW-t ≥ 2；中性化后 ICIR 不低于原来的一半。
- 策略：按候选因子取前 10% 等权、次日开盘进、持 N 日；**样本外净年化 ≥ 10%，且 2025、2026 两个自然年都为正**。
- 达不到 ⇒ 报「未找到」，附全部因子的双向结果，不挑。

## 五、实现落点

`research/panel/daily_ref.py`（xinqing → 日级参考层）· `research/panel/flow_day.py`（trades+orders → 日级微观结构特征）·
`research/eval/factor_eval.py`（IC / 分组 / 换手 / 中性化 / 电池）。产物 `/Volumes/xin/FollowTheBigV2-derived/`。

## 附：外部锚（子 AI 检索，2026-09-03，写在看结果之前）

- A 股 Level-2 日/月频因子的典型量级：RankIC 0.03–0.05，ICIR 1.4–2.5，多空年化 15–30%（天风 2019 订单簿 Spread 因子 IC −4.0%/ICIR −2.10/多空 14.6%；开源 2022 订单级大小单重定标：多空 IR 2.63→4.76，全市场多空年化 27.86%，**方向为正（跟随）**；开源 2024 挂单方向长期记忆：IC 0.037–0.042，ICIR 1.4–2.3，方向未取得）。
- 东方 2021 盘前撤单：「撤单比例越高，未来收益越差」（反向），无公开 IC。
- **未找到公开数字**：收盘集合竞价失衡→次日；被动大单成交；「买入高频撤单率 IC 0.045」的原始出处；整数倍委托指纹。
- 学术界对 A 股订单失衡的**日频**预测力证据不一致（分钟级正向、60–120 分钟反转）。⇒ 我们若在 1–5 日看到稳定同号，本身强于学术基线，要做分域稳健性。

## 六、结果一：盘口特征电池（daily_ref，1122 天，样本内 2022–2024 / 样本外 2025–2026）

- 宇宙、成本、方向按第二节。18 个因子 × 6 个持有期，两份完整表在 `research/eval/out/daily_ref_IS|OOS/battery.md`。
- **`imb10_closecall`（收盘竞价后十档失衡）**：原始 IC 样本内 0.052（fwd_20，ICIR 0.79），样本外同号；**但对 log_amt20 / mom20 / ret1 中性化后样本内翻为 −0.033、样本外 +**。⇒ 它主要是小市值 + 反转的影子，不是独立的 Level-2 信号。
- **`amt_share_closecall`（收盘集合竞价成交占比）**：负向，中性化后样本内外同号（IC −0.017～−0.019，样本外多空 Sharpe −2.4～−2.7）。方向与光大 OCVP 一致。但分钟线即可算，且日频换仓扣费后为负；持 10 日的低档净收益样本外 +11.7%（未验证组合层面）。
- `log_amt20` 负向（小市值），`mom20` 负向（反转）：A 股 2022–2024 的已知基线，样本外仍同号但弱化。
- **结论**：仅靠十档快照的日级聚合，没有一条在中性化后既稳定又能扣费盈利。Level-2 的价值必须在逐笔层（flow 面板）验证。

### 附：事件研究工具冒烟（非预注册，只作工具验证）

`research/eval/event_study.py`，条件「收盘竞价后全簿失衡 > 0.6 ∧ |日收益| < 1% ∧ 成交额 > 1.5 × 20 日均值」，2025-01 起：
每日约 33 个事件，次日开盘进场持 20 日超额 −0.42%（日聚合 NW-t −2.7），胜率 38%。方向是**反向**，与第六节一致。
