# 交接（2026-09-04 第五次 session：探索赚钱模式）

> 先读 `2026-09-03-探索-赚钱模式-预注册.md`（判据 → 结果 → 裁决 → 回测，按时间顺序追加，未改过）。本文只讲状态与下一步。

## 结论一句话

**找到一条在独立确认集上成立的 Level-2 专属信号（发现集 2025–26，确认集 2022–24；措辞按红队意见从「通过预注册」改为此）：「算法拆单执行足迹」。** 单手（100 股）买委托占比高、成交方向 lag-1 自相关高、
盘前撤单比例低、5 分钟收益峰度低 ⇒ 未来 10 日跑赢。2022–2026 五个自然年 IC 全正（确认集 NW-t 8–11），中性化（成交额 / 动量 / 反转 / 股价 / 波动）后成立，
沪深两市都有效、最流动的三分之一最强、10 日后仍在兑现。逐日盯市回测：纯多头相对等权宇宙超额 +7–8%/年（跟踪误差 3–4%，五年全正），
多空 +21–31%/年（Sharpe 3.7–4.2，最大回撤 −5～−6%）。

方向是**跟随**，不是 Fade——但被跟的不是人工庄，是算法。项目命题里「未完成的大资金痕迹」这一条成立，「同一个庄」那一条文献已否定、这里也没用到。

## 数据与代码

- 面板：`/Volumes/xin/FollowTheBigV2-derived/daily_ref/`（1122 天全）、`flow/`（780 天：取样每 3 天一批已全 + 从最近往回补到 2024-02-20；补齐任务曾在 2024 年 2 月初的日子上卡住 7 小时（单进程 29 GB、CPU 满载），重启时若再卡就用 `--days` 跳过那几天）。
- 构建：`research/panel/daily_ref.py`、`research/panel/flow_day.py`（单进程 70 s/天，读盘是底，多进程不提速）。
- 评估：`research/eval/factor_eval.py`（电池）、`composite.py`（合成定义 `SPECS`，已冻结）、`backtest_mtm.py`（逐日盯市）、`event_study.py`。
- 输出：`research/eval/out/`（电池 parquet + md）。
- 门禁：ruff 全过、pytest 全过；`research/` 在 `tools/check_entrypoints.py` 里豁免（探索沙箱）。词汇门禁未跑（需 MINIMAX_API_KEY）。

## 这次 session 的坑

- Sonnet / Codex 子代理都撞了消费上限；opencode（MiniMax）能干活，但**不能写仓库外目录**（会被自动拒，然后静默退出）。
- flow_day 第一版把集合竞价成交漏掉（时间戳带毫秒，`< 15:00:00.000` 排除了 15:00:00.190），撤单金额用了股数——都是「数字看着像对」的错。
- `over("day","symbol")` 改 `over("symbol")` 单进程快 3 倍；三进程反而更慢。
- 评估库第一版远期收益用 close_{t+N}/close_t 直除（除权时假的 −50%），测试的前收盘语义也写反了；已改为复权连乘。
- 发现集/确认集的切分与预注册写的相反（面板从近往远建），已如实记录；确认集 2022–2023 在跑之前没看过。

## 下一步（按价值排）

1. **补齐 flow 面板到日频**（后台在跑）后重跑 `backtest_mtm.py`，看每日换仓下的结果与换手。
2. **红队已跑两轮**（MiniMax、codex），回应在预注册文档第十、十一节；采纳项已修（去 qf_lot1_b、撤前视过滤、t_run_mean 差一）。剩余：行业/ST/限价距离中性化、块 bootstrap。
3. 分市场单独中性化 + 剔除 ST（需名称表，仓库没有）。
4. 冲击成本：前 10% 里流动性最好的三分之一信号最强，做「只买高流动性」版本看容量。
5. 时间尺度：fwd_20 IC 高于 fwd_10，试 hold 20 / 分批。
6. 把 `qf_lot1_b`、`t_sign_ac1`、`pre_cx_b`、`rkurt` 提成注册表条目 / 因子层（现在还在 research/ 沙箱）。

## 分支

`explore/赚钱模式`，PR 见 GitHub。基线 commit 在 `事件定义收口` 分支之上。
