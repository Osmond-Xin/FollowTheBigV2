# 工程管束落地记录：第三步（架构声明先于代码）

- `architecture.toml` 是「图上没有的模块视为不存在」的机器可读形式：11 个模块全部登记（3 个 implemented：core.raw · io.receipt · io.raw；
  8 个 planned：io.events · io.derived · io.runner · core.registry · core.factor · core.portfolio · core.simulate · core.adjudicate · core.stats），
  每个带 package · kind(pure|io) · 架构图名 · depends_on · status。planned 的模块在 src 出现即红（先改 status 再写代码）。
- `tools/check_architecture.py`（自包含 gate）：src 下每个包 ⊆ 声明；从声明生成 import-linter 契约（layers · pure 禁 IO · 每模块 depends_on 的 forbidden）并与 setup.cfg
  生成区块逐字节比对（`--write` 本地重生成，手改即红）；跨模块 import 只能 `from <模块顶层包> import 名字` 且名字在 `__all__` 里，模块内部可深路径；
  禁 `importlib` / `__import__` 指向 ftbv2、禁 `sys.path` 篡改。
- `core.raw` 与 `io.raw` 的 `__init__` 现在是公开接口（`__all__`）；src 与 tools 的跨模块 import 全部改为顶层；tests 不受此约束（测试面可以碰内部）。
- 第四步（文档生成）的架构图模块表将从本文件渲染。
